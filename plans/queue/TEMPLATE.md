# <설계 제목 — 한 줄>
track: factor-research | infra | llm-alpha | trading-live
risk: research-only | live-affecting

---

## 의도 (왜 — 1~3줄)

<!-- AI-DLC: 무엇보다 '왜'가 먼저. 워커가 세부 판단을 내릴 때 이 의도를 기준으로 삼는다.
     예: "백테스트가 느려서 실험 반복이 고통스럽다 → 병목을 줄이고 싶다" -->

## 원하는 것 (델타만)

<!-- 새로 원하는 것만. 기존 기능 설명 금지 — 워커가 digest와 코드에서 파악한다. 3~15줄. -->

## 수용 기준

<!-- 무엇이 확인되면 완료인가. PR에서 항목별 체크박스로 검증된다. 측정 가능하게. -->
- [ ]
- [ ]

## 범위 밖 (non-goals, 선택)

<!-- 헷갈리기 쉬운 인접 작업 중 "이번엔 하지 마라"를 명시 — 워커의 과잉 구현 방지 -->

## 모호할 때 기본 방침 (선택)

<!-- 워커가 스스로 결정해도 되는 방향. 예: "성능보다 가독성 우선", "기존 패턴 최대 재사용",
     "선택지가 갈리면 더 보수적인 쪽". 없으면 워커는 '기존 코드 컨벤션 따르기'가 기본. -->

## 참고 (선택)

<!-- 관련 파일 경로, 과거 실험/메모리 포인터. 없으면 섹션 삭제. -->

<!-- ──────────────────────────────────────────────────────────────
워커와의 계약 (AI-DLC 명확화 루프의 비대화형 번역):
- 사소한 모호함 → 워커가 '기본 방침'에 따라 스스로 결정하되, PR의 "가정" 섹션에 반드시 기록
- 구현을 좌우하는 큰 모호함 → 워커는 추측으로 구현하지 않는다. 질문을 남기고 멈추며,
  Discord로 질문이 전달된다. 답을 이 문서에 보강해 큐에 다시 넣으면 재시도된다.
문서 크기 가이드: 워커 세션 1회(bolt)에 끝날 크기로. 크면 문서를 쪼개라.
─────────────────────────────────────────────────────────────── -->
